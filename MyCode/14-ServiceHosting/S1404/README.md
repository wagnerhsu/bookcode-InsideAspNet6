# README

## Usage

```powershell
dotnet run --  --environment Staging
dotnet run -- /environment=Staging
# 如果后面的参数与dotnet run的参数没有冲突，可以省略--分隔符
dotnet run /environment=Staging
dotnet run --environment=Staging
```
